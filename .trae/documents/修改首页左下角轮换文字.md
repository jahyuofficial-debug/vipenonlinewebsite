# 修改首页左下角轮换文字 — 实施计划

## 目标
修改首页（`index.html`）左下角轮换文字的内容。

## 现状分析
- 首页 banner 区域左下角有轮换文字，由 `bannerData` 对象控制：
  - `topics`：大标题，当前为 `['UUNN→', 'VIBE', 'PULSE', 'FLUX']`
  - `notes`：副标题，当前为 `['DeepDesign', 'Creative Studio', 'Digital Art', 'New Media']`
- 这些文字通过 `changeSlide` 函数定时轮换显示。

## 实施步骤
1. **确认新文字内容**：向用户确认需要替换为哪些新的 topics 和 notes（或只改其中一项）。
2. **修改 `bannerData`**：在 `index.html` 的 `<script>` 区域找到 `bannerData` 定义，替换 `topics` 和/或 `notes` 数组。
3. **验证效果**：启动本地服务器预览，确认左下角轮换文字已按预期更新。

## 文件位置
- `d:\设计文档\TareProcess\Vipen2.0\index.html`（约第 950–953 行）
