# Vipen Project Rules

## Preview Rule

**每次修改代码后，必须启动本地服务器预览效果并提供预览地址给用户。**

- 使用 `npx --yes serve` 在项目目录启动 HTTP 服务（端口 3000）
- 通过 OpenPreview 工具打开预览页面
- 在回复中明确给出预览地址：`http://localhost:3000`
- 若服务已在运行中，直接提供地址即可